# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
gem "jekyll"
# Must specify 1.0.1 because 1.0.2 has a broken dependency (execjs)
gem 'jekyll-autoprefixer', "1.0.1"